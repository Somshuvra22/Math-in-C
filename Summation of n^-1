#include <stdio.h>

int main() {
    int n,i=1;
    float sum=0;
    printf("Number(n):");
    scanf("%d",&n);
    
    for(i;i<=n;i++){
        sum+=(1./i);
    }
    
    printf("The value of 1+1/2+1/3+...+1/n: %.3f",sum);
    
    return 0;
}
